{
  "name": "rafaelsantanna-portfolio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate:content && astro build && npm run validate:dist",
    "preview": "astro preview",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:dist": "node scripts/validate-dist.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.0"
  }
}
